---
name: init
description: Scaffold a Veyra experiment from the catalog
---

If the user named a catalog model, run `python -m veyra init <model>`. Otherwise call `catalog_models` and let them pick. Open the new `.veyra` file. Do not invent scientific numbers.
